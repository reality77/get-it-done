# PocketBase instance URL (no trailing slash)
VITE_PB_URL=http://localhost:8090

# Fixed email for authWithPassword — must match the user you create in PocketBase
# See src/pocketbase/README.md for setup instructions
VITE_PB_USER_EMAIL=user@make-it-done.app
